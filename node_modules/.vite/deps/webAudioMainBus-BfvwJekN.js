import { r as __exportAll } from "./chunk-Bu3CeSBz.js";
import { t as _WebAudioBaseSubGraph } from "./webAudioBaseSubGraph-BLRrO-Z6.js";
import { t as AbstractAudioBus } from "./abstractAudioBus-CHdhL0sh.js";
//#region node_modules/@babylonjs/core/AudioV2/abstractAudio/mainAudioBus.js
/**
* Abstract class representing a main audio bus.
*
* Main audio buses are the last bus in the audio graph.
*
* Unlike {@link AudioBus} instances, `MainAudioBus` instances have no spatial audio and stereo output capabilities,
* and they cannot be connected downstream to another audio bus. They only connect downstream to the audio engine's
* main output.
*
* Main audio buses are created by the {@link CreateMainAudioBusAsync} function.
*/
var MainAudioBus = class extends AbstractAudioBus {
	constructor(name, engine) {
		super(name, engine);
	}
};
//#endregion
//#region node_modules/@babylonjs/core/AudioV2/webAudio/webAudioMainBus.js
var webAudioMainBus_exports = /* @__PURE__ */ __exportAll({ _WebAudioMainBus: () => _WebAudioMainBus });
/** @internal */
var _WebAudioMainBus = class _WebAudioMainBus extends MainAudioBus {
	/** @internal */
	constructor(name, engine) {
		super(name, engine);
		this._subGraph = new _WebAudioMainBus._SubGraph(this);
	}
	/** @internal */
	async _initAsync(options) {
		await this._subGraph.initAsync(options);
		if (this.engine.mainOut) {
			if (!this._connect(this.engine.mainOut)) throw new Error("Connect failed");
		}
		this.engine._addMainBus(this);
	}
	/** @internal */
	dispose() {
		super.dispose();
		this.engine._removeMainBus(this);
	}
	/** @internal */
	get _inNode() {
		return this._subGraph._inNode;
	}
	/** @internal */
	get _outNode() {
		return this._subGraph._outNode;
	}
	_connect(node) {
		if (!super._connect(node)) return false;
		if (node._inNode) this._outNode?.connect(node._inNode);
		return true;
	}
	_disconnect(node) {
		if (!super._disconnect(node)) return false;
		if (node._inNode) this._outNode?.disconnect(node._inNode);
		return true;
	}
	/** @internal */
	getClassName() {
		return "_WebAudioMainBus";
	}
};
_WebAudioMainBus._SubGraph = class extends _WebAudioBaseSubGraph {
	get _downstreamNodes() {
		return this._owner._downstreamNodes ?? null;
	}
};
//#endregion
export { webAudioMainBus_exports as n, MainAudioBus as r, _WebAudioMainBus as t };

//# sourceMappingURL=webAudioMainBus-BfvwJekN.js.map