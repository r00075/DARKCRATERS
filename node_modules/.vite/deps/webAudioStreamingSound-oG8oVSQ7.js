import { r as __exportAll } from "./chunk-Bu3CeSBz.js";
import { n as Observable } from "./observable-Dg5nDoxE.js";
import { t as Logger } from "./logger-DkPtg8o-.js";
import { n as Tools } from "./tools-BCxOqog4.js";
import { t as WebRequest } from "./webRequest-IHAPaBGJ.js";
import { a as _CleanUrl, s as _GetUrlForStreaming } from "./abstractAudioNode-BTtAuHRD.js";
import { c as _HasSpatialAudioOptions, n as _WebAudioBusAndSoundSubGraph, r as _StereoAudio, t as _SpatialWebAudio } from "./spatialWebAudio-CK3hUO5S.js";
import { n as AbstractSound, t as _AbstractSoundInstance } from "./abstractSoundInstance-B8vss5Vs.js";
//#region node_modules/@babylonjs/core/AudioV2/abstractAudio/streamingSound.js
/**
* Abstract class representing a streaming sound.
*
* A streaming sound has a sound buffer that is loaded into memory in chunks as it is played. This allows it to be played
* more quickly than a static sound, but it also means that it cannot have loop points or playback rate changes.
*
* Due to the way streaming sounds are typically implemented, there can be a significant delay when attempting to play
* a streaming sound for the first time. To prevent this delay, it is recommended to preload instances of the sound
* using the {@link IStreamingSoundStoredOptions.preloadCount} options, or the {@link preloadInstanceAsync} and
* {@link preloadInstancesAsync} methods before calling the `play` method.
*
* Streaming sounds are created by the {@link CreateStreamingSoundAsync} function.
*/
var StreamingSound = class extends AbstractSound {
	constructor(name, engine, options) {
		super(name, engine, options);
		this._preloadedInstances = new Array();
	}
	/**
	* The number of instances to preload. Defaults to `1`.
	*/
	get preloadCount() {
		return this._options.preloadCount ?? 1;
	}
	/**
	* Returns the number of instances that have been preloaded.
	*/
	get preloadCompletedCount() {
		return this._preloadedInstances.length;
	}
	/**
	* Preloads an instance of the sound.
	* @returns A promise that resolves when the instance is preloaded.
	*/
	preloadInstanceAsync() {
		const instance = this._createInstance();
		this._addPreloadedInstance(instance);
		return instance.preloadedPromise;
	}
	/**
	* Preloads the given number of instances of the sound.
	* @param count - The number of instances to preload.
	* @returns A promise that resolves when all instances are preloaded.
	*/
	async preloadInstancesAsync(count) {
		for (let i = 0; i < count; i++) this.preloadInstanceAsync();
		await Promise.all(this._preloadedInstances.map(async (instance) => await instance.preloadedPromise));
	}
	/**
	* Plays the sound.
	* - Triggers `onEndedObservable` if played for the full duration and the `loop` option is not set.
	* @param options The options to use when playing the sound. Options set here override the sound's options.
	*/
	play(options = {}) {
		if (this.state === 5) {
			this.resume();
			return;
		}
		let instance;
		if (this.preloadCompletedCount > 0) {
			instance = this._preloadedInstances[0];
			instance.startOffset = this.startOffset;
			this._removePreloadedInstance(instance);
		} else instance = this._createInstance();
		const onInstanceStateChanged = () => {
			if (instance.state === 3) {
				this._stopExcessInstances();
				instance.onStateChangedObservable.removeCallback(onInstanceStateChanged);
			}
		};
		instance.onStateChangedObservable.add(onInstanceStateChanged);
		options.startOffset ?? (options.startOffset = this.startOffset);
		options.loop ?? (options.loop = this.loop);
		options.volume ?? (options.volume = 1);
		this._beforePlay(instance);
		instance.play(options);
		this._afterPlay(instance);
	}
	/**
	* Stops the sound.
	*/
	stop() {
		this._setState(1);
		if (!this._instances) return;
		for (const instance of Array.from(this._instances)) instance.stop();
	}
	_addPreloadedInstance(instance) {
		if (!this._preloadedInstances.includes(instance)) this._preloadedInstances.push(instance);
	}
	_removePreloadedInstance(instance) {
		const index = this._preloadedInstances.indexOf(instance);
		if (index !== -1) this._preloadedInstances.splice(index, 1);
	}
};
//#endregion
//#region node_modules/@babylonjs/core/AudioV2/abstractAudio/streamingSoundInstance.js
/** @internal */
var _StreamingSoundInstance = class extends _AbstractSoundInstance {
	constructor(sound) {
		super(sound);
		/** @internal */
		this.onReadyObservable = new Observable();
		/** @internal */
		this.preloadedPromise = new Promise((resolve, reject) => {
			this._rejectPreloadedPromise = reject;
			this._resolvePreloadedPromise = resolve;
		});
		this.onErrorObservable.add(this._rejectPreloadedPromise);
		this.onReadyObservable.add(this._resolvePreloadedPromise);
	}
	/** @internal */
	set startOffset(value) {
		this._options.startOffset = value;
	}
	/** @internal */
	dispose() {
		super.dispose();
		this.onErrorObservable.clear();
		this.onReadyObservable.clear();
		this._resolvePreloadedPromise();
	}
};
//#endregion
//#region node_modules/@babylonjs/core/AudioV2/webAudio/webAudioStreamingSound.js
var webAudioStreamingSound_exports = /* @__PURE__ */ __exportAll({ _WebAudioStreamingSound: () => _WebAudioStreamingSound });
/** @internal */
var _WebAudioStreamingSound = class _WebAudioStreamingSound extends StreamingSound {
	/** @internal */
	constructor(name, engine, options) {
		super(name, engine, options);
		this._stereo = null;
		this._options = {
			autoplay: options.autoplay ?? false,
			loop: options.loop ?? false,
			maxInstances: options.maxInstances ?? Infinity,
			preloadCount: options.preloadCount ?? 1,
			startOffset: options.startOffset ?? 0
		};
		this._subGraph = new _WebAudioStreamingSound._SubGraph(this);
	}
	/** @internal */
	async _initAsync(source, options) {
		const audioContext = this.engine._audioContext;
		if (!(audioContext instanceof AudioContext)) throw new Error("Unsupported audio context type.");
		this._audioContext = audioContext;
		this._source = source;
		if (options.outBus) this.outBus = options.outBus;
		else if (options.outBusAutoDefault !== false) {
			await this.engine.isReadyPromise;
			this.outBus = this.engine.defaultMainBus;
		}
		await this._subGraph.initAsync(options);
		if (_HasSpatialAudioOptions(options)) this._initSpatialProperty();
		if (this.preloadCount) await this.preloadInstancesAsync(this.preloadCount);
		if (options.autoplay) this.play(options);
		this.engine._addSound(this);
	}
	/** @internal */
	get _inNode() {
		return this._subGraph._inNode;
	}
	/** @internal */
	get _outNode() {
		return this._subGraph._outNode;
	}
	/** @internal */
	get stereo() {
		return this._stereo ?? (this._stereo = new _StereoAudio(this._subGraph));
	}
	/** @internal */
	dispose() {
		super.dispose();
		this._stereo = null;
		this._subGraph.dispose();
		this.engine._removeSound(this);
	}
	/** @internal */
	getClassName() {
		return "_WebAudioStreamingSound";
	}
	_createInstance() {
		return new _WebAudioStreamingSoundInstance(this, this._options);
	}
	_connect(node) {
		if (!super._connect(node)) return false;
		if (node._inNode) this._outNode?.connect(node._inNode);
		return true;
	}
	_disconnect(node) {
		if (!super._disconnect(node)) return false;
		if (node._inNode) this._outNode?.disconnect(node._inNode);
		return true;
	}
	_createSpatialProperty(autoUpdate, minUpdateTime) {
		return new _SpatialWebAudio(this._subGraph, autoUpdate, minUpdateTime);
	}
	_getOptions() {
		return this._options;
	}
};
_WebAudioStreamingSound._SubGraph = class extends _WebAudioBusAndSoundSubGraph {
	get _downstreamNodes() {
		return this._owner._downstreamNodes ?? null;
	}
	get _upstreamNodes() {
		return this._owner._upstreamNodes ?? null;
	}
};
/** @internal */
var _WebAudioStreamingSoundInstance = class extends _StreamingSoundInstance {
	constructor(sound, options) {
		super(sound);
		this._currentTimeChangedWhilePaused = false;
		this._enginePlayTime = Infinity;
		this._enginePauseTime = 0;
		this._isReady = false;
		this._isReadyPromise = new Promise((resolve, reject) => {
			this._resolveIsReadyPromise = resolve;
			this._rejectIsReadyPromise = reject;
		});
		this._onCanPlayThrough = () => {
			this._isReady = true;
			this._resolveIsReadyPromise(this._mediaElement);
			this.onReadyObservable.notifyObservers(this);
		};
		this._onEnded = () => {
			this._setState(1);
		};
		this._onError = (reason) => {
			this._setState(4);
			this.onErrorObservable.notifyObservers(reason);
			this._rejectIsReadyPromise(reason);
			this.dispose();
		};
		this._onEngineStateChanged = () => {
			if (this.engine.state !== "running") return;
			if (this._options.loop && this.state === 2) this.play();
			this.engine.stateChangedObservable.removeCallback(this._onEngineStateChanged);
		};
		this._onUserGesture = () => {
			this.play();
		};
		this._options = options;
		this._volumeNode = new GainNode(sound._audioContext);
		if (typeof sound._source === "string") this._initFromUrl(sound._source);
		else if (Array.isArray(sound._source)) this._initFromUrls(sound._source);
		else if (sound._source instanceof HTMLMediaElement) this._initFromMediaElement(sound._source);
		else throw new Error(`Invalid streaming sound source (${sound._source}).`);
	}
	/** @internal */
	get currentTime() {
		if (this._state === 1) return 0;
		const timeSinceLastStart = this._state === 5 ? 0 : this.engine.currentTime - this._enginePlayTime;
		return this._enginePauseTime + timeSinceLastStart + this._options.startOffset;
	}
	set currentTime(value) {
		const restart = this._state === 2 || this._state === 3;
		if (restart) {
			this._mediaElement.pause();
			this._state = 1;
		}
		this._options.startOffset = value;
		if (restart) this.play({ startOffset: value });
		else if (this._state === 5) this._currentTimeChangedWhilePaused = true;
	}
	get _outNode() {
		return this._volumeNode;
	}
	/** @internal */
	get startTime() {
		if (this._state === 1) return 0;
		return this._enginePlayTime;
	}
	/** @internal */
	dispose() {
		super.dispose();
		this.stop();
		this._sourceNode?.disconnect(this._volumeNode);
		this._sourceNode = null;
		this._mediaElement.removeEventListener("error", this._onError);
		this._mediaElement.removeEventListener("ended", this._onEnded);
		this._mediaElement.removeEventListener("canplaythrough", this._onCanPlayThrough);
		for (const source of Array.from(this._mediaElement.children)) this._mediaElement.removeChild(source);
		this.engine.stateChangedObservable.removeCallback(this._onEngineStateChanged);
		this.engine.userGestureObservable.removeCallback(this._onUserGesture);
	}
	/** @internal */
	play(options = {}) {
		if (this._state === 3) return;
		if (options.loop !== void 0) this._options.loop = options.loop;
		this._mediaElement.loop = this._options.loop;
		let startOffset = options.startOffset;
		if (this._currentTimeChangedWhilePaused) {
			startOffset = this._options.startOffset;
			this._currentTimeChangedWhilePaused = false;
		} else if (this._state === 5) startOffset = this.currentTime;
		if (startOffset && startOffset > 0) this._mediaElement.currentTime = startOffset;
		this._volumeNode.gain.value = options.volume ?? 1;
		this._play();
	}
	/** @internal */
	pause() {
		if (this._state !== 2 && this._state !== 3) return;
		this._setState(5);
		this._enginePauseTime += this.engine.currentTime - this._enginePlayTime;
		this._mediaElement.pause();
	}
	/** @internal */
	resume() {
		if (this._state === 5) this.play();
		else if (this._currentTimeChangedWhilePaused) this.play();
	}
	/** @internal */
	stop() {
		if (this._state === 1) return;
		this._stop();
	}
	/** @internal */
	getClassName() {
		return "_WebAudioStreamingSoundInstance";
	}
	_connect(node) {
		if (!super._connect(node)) return false;
		if (node instanceof _WebAudioStreamingSound && node._inNode) this._outNode?.connect(node._inNode);
		return true;
	}
	_disconnect(node) {
		if (!super._disconnect(node)) return false;
		if (node instanceof _WebAudioStreamingSound && node._inNode) this._outNode?.disconnect(node._inNode);
		return true;
	}
	_initFromMediaElement(mediaElement) {
		Tools.SetCorsBehavior(mediaElement.currentSrc, mediaElement);
		mediaElement.controls = false;
		mediaElement.loop = this._options.loop;
		mediaElement.preload = "auto";
		mediaElement.addEventListener("canplaythrough", this._onCanPlayThrough, { once: true });
		mediaElement.addEventListener("ended", this._onEnded, { once: true });
		mediaElement.addEventListener("error", this._onError, { once: true });
		mediaElement.load();
		this._sourceNode = new MediaElementAudioSourceNode(this._sound._audioContext, { mediaElement });
		this._sourceNode.connect(this._volumeNode);
		if (!this._connect(this._sound)) throw new Error("Connect failed");
		this._mediaElement = mediaElement;
	}
	_initFromUrl(url) {
		const resolvedUrl = WebRequest.IsCustomRequestAvailable ? _GetUrlForStreaming(_CleanUrl(url)) : _CleanUrl(url);
		const audio = new Audio(resolvedUrl);
		this._initFromMediaElement(audio);
	}
	_initFromUrls(urls) {
		const audio = new Audio();
		for (const url of urls) {
			const source = document.createElement("source");
			source.src = WebRequest.IsCustomRequestAvailable ? _GetUrlForStreaming(_CleanUrl(url)) : _CleanUrl(url);
			audio.appendChild(source);
		}
		this._initFromMediaElement(audio);
	}
	_play() {
		this._setState(2);
		if (!this._isReady) {
			this._playWhenReady();
			return;
		}
		if (this._state !== 2) return;
		if (this.engine.state === "running") {
			const result = this._mediaElement.play();
			this._enginePlayTime = this.engine.currentTime;
			this._setState(3);
			result.catch(() => {
				this._setState(4);
				if (this._options.loop) this.engine.userGestureObservable.addOnce(this._onUserGesture);
			});
		} else if (this._options.loop) this.engine.stateChangedObservable.add(this._onEngineStateChanged);
		else {
			this.stop();
			this._setState(4);
		}
	}
	_playWhenReady() {
		this._isReadyPromise.then(() => {
			this._play();
		}).catch(() => {
			Logger.Error("Streaming sound instance failed to play");
			this._setState(4);
		});
	}
	_stop() {
		this._mediaElement.pause();
		this._onEnded();
		this.engine.stateChangedObservable.removeCallback(this._onEngineStateChanged);
	}
};
//#endregion
export { webAudioStreamingSound_exports as n, StreamingSound as r, _WebAudioStreamingSound as t };

//# sourceMappingURL=webAudioStreamingSound-oG8oVSQ7.js.map