import { r as __exportAll } from "./chunk-Bu3CeSBz.js";
import { r as RegisterClass } from "./typeStore-Dy-yCYbL.js";
import { u as RichTypeAny } from "./flowGraphBlock-BWpYsKc5.js";
import { t as FlowGraphExecutionBlockWithOutSignal } from "./flowGraphExecutionBlockWithOutSignal-C5lh6ncI.js";
//#region node_modules/@babylonjs/core/FlowGraph/Blocks/Execution/Audio/flowGraphPauseSoundBlock.js
var flowGraphPauseSoundBlock_exports = /* @__PURE__ */ __exportAll({ FlowGraphPauseSoundBlock: () => FlowGraphPauseSoundBlock });
/**
* @experimental
* A block that pauses an Audio V2 sound. If the sound is already paused, resumes it.
*/
var FlowGraphPauseSoundBlock = class extends FlowGraphExecutionBlockWithOutSignal {
	/**
	* Constructs a new FlowGraphPauseSoundBlock.
	* @param config - optional configuration for the block
	*/
	constructor(config) {
		super(config);
		this.sound = this.registerDataInput("sound", RichTypeAny);
	}
	/**
	* @internal
	*/
	_execute(context, _callingSignal) {
		const soundValue = this.sound.getValue(context);
		if (!soundValue) {
			this._reportError(context, "No sound provided");
			this.out._activateSignal(context);
			return;
		}
		if (soundValue.state === 5) soundValue.resume();
		else if (soundValue.state === 2 || soundValue.state === 3) soundValue.pause();
		this.out._activateSignal(context);
	}
	/**
	* @returns class name of the block.
	*/
	getClassName() {
		return "FlowGraphPauseSoundBlock";
	}
};
RegisterClass("FlowGraphPauseSoundBlock", FlowGraphPauseSoundBlock);
//#endregion
export { flowGraphPauseSoundBlock_exports as n, FlowGraphPauseSoundBlock as t };

//# sourceMappingURL=flowGraphPauseSoundBlock-CPsa-VJF.js.map