import { r as __exportAll } from "./chunk-Bu3CeSBz.js";
import { r as RegisterClass } from "./typeStore-Dy-yCYbL.js";
import { t as FlowGraphExecutionBlock } from "./flowGraphExecutionBlock-MpOf-UAt.js";
//#region node_modules/@babylonjs/core/FlowGraph/Blocks/Execution/ControlFlow/flowGraphSequenceBlock.js
var flowGraphSequenceBlock_exports = /* @__PURE__ */ __exportAll({ FlowGraphSequenceBlock: () => FlowGraphSequenceBlock });
/**
* A block that executes its output flows in sequence.
*/
var FlowGraphSequenceBlock = class extends FlowGraphExecutionBlock {
	constructor(config) {
		super(config);
		this.config = config;
		/**
		* The output flows.
		*/
		this.executionSignals = [];
		this.setNumberOfOutputSignals(this.config.outputSignalCount);
	}
	_execute(context) {
		for (let i = 0; i < this.executionSignals.length; i++) this.executionSignals[i]._activateSignal(context);
	}
	/**
	* Sets the block's output flows. Would usually be passed from the constructor but can be changed afterwards.
	* @param outputSignalCount the number of output flows
	*/
	setNumberOfOutputSignals(outputSignalCount = 1) {
		while (this.executionSignals.length > outputSignalCount) {
			const flow = this.executionSignals.pop();
			if (flow) {
				flow.disconnectFromAll();
				this._unregisterSignalOutput(flow.name);
			}
		}
		while (this.executionSignals.length < outputSignalCount) this.executionSignals.push(this._registerSignalOutput(`out_${this.executionSignals.length}`));
	}
	/**
	* @returns class name of the block.
	*/
	getClassName() {
		return "FlowGraphSequenceBlock";
	}
};
RegisterClass("FlowGraphSequenceBlock", FlowGraphSequenceBlock);
//#endregion
export { flowGraphSequenceBlock_exports as n, FlowGraphSequenceBlock as t };

//# sourceMappingURL=flowGraphSequenceBlock-BJFe4tEd.js.map